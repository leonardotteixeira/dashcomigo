{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "npm install && npm run build && cd api && node src/server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}

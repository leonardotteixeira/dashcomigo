{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd api && npm install"
  },
  "deploy": {
    "startCommand": "cd api && node src/server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
